using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Square_root : MonoBehaviour, Enemy
{

    //TODO Add a raycast and extend the spear when the player is close to the enemy

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
